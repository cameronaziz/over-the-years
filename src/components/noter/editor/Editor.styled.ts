import styled from 'styled-components'
import { StyledContainer } from '../noter.styles'

export const StyledEditorContainer = styled(StyledContainer)`
  font-family: Consolas, "Liberation Mono", Courier, monospace;
`
